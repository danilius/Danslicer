# Task 06 — resin volume and print time

In progress on `codex/ui-refresh-06`, worktree `C:/Users/plane/.codex/worktrees/271f/Danslicer-chatgpt`.
Verified clean baseline/last good commit: `dc4664cd7321030cfe04b866815427b6b4f8a531`.

Plan: audit sliced polygon union, AA and crop semantics; expose compact Slicing-page estimates with explicit assumptions; fix stale asynchronous completion; validate deterministic volume/timing and native UI with isolated config/output; commit evidence and recovery instructions.

Task 07 runs independently from the SAME task-05 baseline. Its report must be deliberately imported by later coordination; this branch does not implement printer support. This overrides historical sequential task-06/task-07 queue wording. No main merge/push or other checkout writes. Physical SpaceMouse retest remains unverified.
