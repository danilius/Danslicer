# Task 03 — settings integration and persistence

## Start — 2026-09-10
Worktree: C:/Users/plane/.codex/worktrees/3321/Danslicer-chatgpt
Branch: codex/ui-refresh-03
Last good commit/base: a0fed8b933f3358e875e4650bbe8915d5a57c574 (clean exact task-02 handoff).
User confirmed task 02 is done; this supersedes its historical pending-review language.
Plan: separate compatible workspace preferences file; isolated capture configuration; production rectangular numeric controls through existing model setters; preserve undo, units, commands and the approved Support isolation exception. Validate corrupt/stale preference recovery, numeric transactions, native main workflows and screenshots. No renderer/core geometry edits, other checkout changes, main merge or later task implementation.
Initial checks: exact HEAD and clean status verified, dedicated branch created (Git metadata requires escalation because shared .git lives outside this worktree). No implementation checks yet.
Next: implement persistence and numeric integration, build and test.

## Iteration 1 — production binding and durable workspace state
Worktree: C:/Users/plane/.codex/worktrees/3321/Danslicer-chatgpt; branch codex/ui-refresh-03.
Last good committed checkpoint: e18b37a (plan).
Changed: AppConfig/App startup isolate native capture config; WorkspacePreferences sidecar and MainWindow.Preferences restore/persist toolbar, widths, recent projects, section order/expansion; ToolShells reports completed resize and cancels capture loss; ModelScrubField/NumericField retain one existing model callback; MainWindow templates and support/raft bounded numeric controls use centered in-field units; new persistence tests/native settings checks.
Checks: initial Release solution build passed (existing warnings); latest build plus 171 focused tests passed, 0 failed/skipped. Restricted restore required approved NuGet access. First updated native main capture failed in the unchanged task-02 isolation delayed-dismiss assertion before reaching new settings checks; investigate deterministic dispatcher timing. No visual pass claimed yet. Support config setters retain existing immediate-save semantics and no document undo; transform edits use existing document undo. Visibility has no numeric parameter, so existing modes/switches remain.
Next: resolve native capture timing, exercise settings/persistence interactions, inspect screenshots and commit verified refinement. No renderer/core geometry edits.

## Iteration 2 — verified native interactions and refinement
Worktree: C:/Users/plane/.codex/worktrees/3321/Danslicer-chatgpt; branch codex/ui-refresh-03.
Last good committed checkpoint: e2a77df (integration). This iteration's exact commit is the next documentation checkpoint.
Changed: completed region/placement scrub integration; added accessible field names and detached model-listener cleanup; integer controls normalize commits and retain whole keyboard steps with Shift. Added process-wide temporary configuration for all unit-test VMs. Native checks caught a resize-cancel race when preferred Width differed from measured Bounds; cancellation now restores the preferred width, and commits alone save it. Isolation test now permits up to two seconds for background dispatcher latency while still checking the 100ms gap and retaining the production 300ms timer unchanged.
Actual results: Release solution/app build passed, existing warnings only. 181 focused tests passed, 0 failures/skips, including compatibility/persistence, numeric parser/model, object position/undo, project/command/mode/keymap and isolation checks. Final native MAIN smoke and control-preview smoke each exited 0. Main checks cover actual transform drag previews/one commit/single undo/cancel, cm/mm expressions, invalid and range rejection, blur commit, no-op suppression, integer rounding/keyboard, support setter called exactly once, real raft thickness, config/cap-off round trip, new-window toolbar/width/section restoration, recents, resize cancellation, all 12 popouts, Support rail and narrow layout. Control smoke includes locks, whole-section drag/cancel/150ms settle and resize.
Visual review: inspected transform, support, raft, print, narrow print and narrow isolation captures. 100/150/200 density renders produced. GL composition omitted by offscreen rendering; physical mouse/monitor DPI/screen-reader/light-theme tests remain unverified. Dark themes only. Status bar subtree identical after newline normalization; no Core, Render or ViewportControl changes; git diff whitespace check passed.
Next: commit source/evidence and final handoff. Task 04 owns renderer/plate/reflections/below transparency/AO/cavity and only a verified bevel correction. No later task created or main merge.

## Final checkpoint
Same absolute worktree and branch. Last good implementation/evidence dfc760f; final descendant also changes AppConfig to thread-safe Lazy initialization before normal startup or isolated tests. Final 181 focused tests and native main capture passed again after that change. Source, native evidence and final documentation committed; exact handoff hash in final response. See RESUME for launch, limits and exact next action.
Focused tests: `dotnet test tests/Danslicer.Tests -c Release --no-restore -p:UsedAvaloniaProducts= --filter 'FullyQualifiedName~Workspace|FullyQualifiedName~UiRefreshNumeric|FullyQualifiedName~Expression|FullyQualifiedName~ConfigViewModel|FullyQualifiedName~UserConfig|FullyQualifiedName~LayerRange|FullyQualifiedName~IsolationHeight|FullyQualifiedName~ClipCap|FullyQualifiedName~ViewportToolbar|FullyQualifiedName~WindowKeymap|FullyQualifiedName~ModeScoped|FullyQualifiedName~ObjectCommandScope|FullyQualifiedName~ProjectFile|FullyQualifiedName~NewProject|FullyQualifiedName~SliceCommand|FullyQualifiedName~ObjectPosition|FullyQualifiedName~UndoStack' --nologo`.

## User refinement — swap while dragging
Worktree C:/Users/plane/.codex/worktrees/3321/Danslicer-chatgpt, branch codex/ui-refresh-03, clean base 97eedfc404614494e0fea4cab382b6b4ff2c3459.
User requested immediate swapping when a viable expander target is reached. Plan: swap on neighbour midpoint crossing during movement, maintain pointer anchoring across layout changes, restore initial order on Escape/capture loss, persist only on drop (keyboard moves remain immediate). Verify native live swap, reversal/cancel, settle and persistence. Next: implement and run native checks.

### Live swap — verified
Same worktree/branch; last good prior handoff 97eedfc404614494e0fea4cab382b6b4ff2c3459. Neighbour midpoint crossing now swaps the real expanded sections during pointer movement; fast movement can cross multiple sections. Layout compensation keeps the dragged section anchored, and the existing 150ms release settling remains. Escape and capture loss restore initial order; live reversal works. OrderCommitted now separates durable drop/keyboard changes from transient MoveRequested swaps. No provisional order is saved.
Changed ToolShells.cs, MainWindow.Preferences.cs, native MainWindow.SettingsCapture.cs and UiPreviewWindow.cs, docs/evidence. Release app build passed (existing warnings). Native preview and full native main smoke exited 0, with new checks for pre-release swap, pointer anchoring, reversal, Escape/capture-loss restoration and unchanged preference file until drop. Reviewed preview-drag.png in evidence/task-03-live-swap. Physical pointer/DPI/GL limits unchanged. Existing 181 unit-test result is historical; no unit rerun for this routed UI change. Next: user review in this same main app; task-04 boundaries unchanged. Final commit hash is in response.
