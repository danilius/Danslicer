# Task 04 — plate and depth readability

Worktree: C:/Users/plane/.codex/worktrees/e262/Danslicer-chatgpt
Branch: codex/ui-refresh-04
Exact clean task-03 base / last-good commit: 8a0fac4f52515f18f1560a54b938dbb2b8f17a60.

## Plan and committed overlap audit — 2026-09-10
Verified detached HEAD at the requested base and clean status; created the dedicated branch. Read START, SPEC, TASKS, RESUME, CLAUDE-NOTE and prior task notes. Shared repository history only was inspected; no source from another checkout was read or copied. Main remains 7d95ceb. Renderer/raft commits already in the base include plate angle fade 1767634, shadows bc6eb18, and outward-only scraper lip ebfb13e. Tasks 01–03 did not change renderer/core geometry.

Existing plate is a flat quad at -0.05 mm; fading uses direction and eye height, defaults to 0.3 residual opacity. Shadows switch off as soon as fading starts. Deferred has four normal-neighbour cavity samples, outlines and FXAA, but no proximity AO. Classic has studio lighting, no cavity/AO. Raft mesh and slicing explicitly widen the outer top edge and keep holes vertical; validate existing tests before considering a correction.

Next: add an opt-in native GL framebuffer evidence harness isolated before config loading; capture exact baseline renderer above/below/contact. Implement shallow plate, restrained configurable rough reflections and complete below removal; add proximity AO to deferred while retaining cavity and honest Classic limits. Validate actual framebuffer output, picking, clipping, transparency, effects toggles and cost. Run geometry/renderer/UI regression tests and Release build. Preserve all task-03 contracts; no task 05 or main merge.

No implementation or visual checks passed yet.

## Baseline framebuffer checkpoint
Native GL harness renders deterministic scene/aux contacts through the production renderer, reads host framebuffer RGBA, verifies deferred did not fall back and GPU selection reaches the model from below. Initial pick target was obscured by a fixture pillar; moved to an unobstructed underside point. Both paths completed all seven views, GL error checks passed. ANGLE ES 3.0 on NVIDIA RTX 4090; 1000x760. Baseline timing 0.12–0.57 ms/frame with glFinish (tiny synthetic scene, not a production benchmark). Actual rendered images are evidence/task-04-before; above image reviewed. Build passed after qualifying PixelFormat; existing viewport warnings only. Initial sandbox restore was denied network sockets, approved restore succeeded. No production renderer edits yet. Last good plan commit bda9772; next implement shared plate/reflections and deferred AO.

## Renderer implementation checkpoint
Last good committed baseline: 2446a42. Same worktree/branch. Added viewport-only 2 mm closed chamfered plate (top still -0.05 mm), smooth angle/eye-height surface fade, smooth pre-fade shadow attenuation, grid fade and optional below perimeter. Legacy PlateOpacityFromBelow JSON value now controls perimeter only; saved value remains intact and the preferences label states its new role. No raft/slicing/support-generation source changed.

Shared quarter-resolution RGBA8/depth24 mirrored scene target plus nine weighted blur taps provides restrained reflections on both render paths. Default 0.12, maximum 0.3; omitted entirely below/off. Opaque visible model/aux geometry only, respecting isolation; no ghost/transparent/reflection picking. Plate has broad satin specular response. Fixed faded-plate ordering before transparent aux in both paths and enabled convex-skin culling during plate blending.

Deferred AO adds 16 bounded view-space depth samples (default 2 mm radius, 0.35 strength) to the existing composite; independent of the existing four normal-sample cavity. No new full-resolution attachment or pass for AO. Normal alpha identifies plate material for restrained specular. View/menu toggles and preferences strengths/radius use existing saving conventions; new preference fields use filled numeric controls. Classic retains its existing studio lighting without AO/cavity; this is explicit, not an AO implementation claim.

Actual checks: Release app builds passed; 280 targeted tests passed, 0 failed/skipped, including raft mesh/slicing outward-lip tests, new closed outward slab/continuous fade/config tests, renderer/clipping/support mesh and task-03 UI contracts. Native main smoke completed with workspace-ok.txt. Native GL after capture passed on both paths without errors/fallback; below GPU picking passed. Inspected contact, below, reflection-on and Classic above images. Quantitative comparisons detect independent deferred AO/cavity and shared reflections; below reflection toggles are byte-identical. First implementation had shadows overwriting the reflection material and translucent-support ordering inherited from baseline; both corrected before latest after capture. No raft mismatch confirmed, so no geometry correction or geometry commit.

Next: expand real support/cap and native view-toggle evidence, final visual/regression review, solution build and clean handoff. Tiny-scene GL timing is not a dense production benchmark. Prior physical DPI/mouse/screen-reader limits remain.

## Isolation and final evidence refinement
Last-good implementation commit 30d4bdb, same e262 worktree / codex/ui-refresh-04. Expanded the GL harness with production SupportRenderMesh cone/disc/trunk meshes (15 supports), a 600-support / 422400-triangle fixture, orthographic/MatCap/selection/transparent-grazing views, exact Classic sliced caps and deferred painted caps above/below. Added independent pixel-difference assertions for effects, below reflection identity and cap on/off. Main smoke now checks saved AO/reflection toggle round trips, menu/popout agreement and unchanged saved Cap.

Actual real-support capture exposed pre-existing painted-cap stencil residue: geometry wholly outside the named plane contributed floating cap marks. Backface culling alone did not remove them. The final renderer fix excludes solids whose world bounds do not cross that plane and culls the cap quad's reverse face. Final above/below images inspected: phantom discs/noise gone; only actual cuts change when Cap toggles. Automated framebuffer assertion requires the lower half of the above fixture to stay byte-identical between Cap on/off. No slicing geometry changed. Preserved pre-fix image as painted-cap-before-fix.png.

Plate material now stays satin Studio when model MatCap changes. Plate AO/cavity fade out before its opaque/deferred to translucent/forward transition; its existing border supplies the perimeter without a discontinuous deferred plate outline.

Final solution build passed. 50 focused cap/plate/raft/render-config tests passed after the correction (earlier broad 280-test run remains valid for unchanged UI/core contracts). Final GL run completed 58 views, all GL errors/picking/effect-difference/cap-leak assertions passed; main smoke re-run passed, including new view controls. Native control preview passed, preserving gripper threshold, pointer anchoring/cancel/250ms displaced easing and save-on-release tests. Status-bar subtree compared identical to 8a0fac4 after newline normalization; no support/slicing/ToolShells/persistence/isolation source diff. Existing compiler/analyzer warnings only.

Timings now record median/p95/max over 30 glFinish-synchronized frames after 12 warmups at 1000x760 on ANGLE ES3 / RTX4090. Latest tiny contact: deferred median 0.28 ms enabled / 0.19 off; Classic 0.13 / 0.09. Dense fixture: deferred 0.22 / 0.16 ms; Classic 0.22 / 0.15. These are in-process render submission + completion measurements, not end-to-end interaction latency or portable GPU guarantees. Prior short runs had outliers up to 15 ms; retain this limitation and use the detailed final result.txt.


## Final recovery checkpoint and commands
Same worktree/branch. Last-good source/evidence: cafca37cbc74f9f0bf21094271f2b2bb07961f1f. Final documentation-only descendant is the handoff HEAD; exact full ref in final response. Read RESUME for launch, controls, effect costs/limits and task-05 review instructions. The initial status-bar audit script used the wrong XAML property spelling (DockPanel instead of DockPanel.Dock); corrected audit then passed, confirming identical status content. No unresolved renderer failure remains in exercised fixtures. Physical/alternate-GPU/full-job limits remain explicit.

Broad regression command (280 passed):
```powershell
dotnet test tests/Danslicer.Tests -c Release -p:UsedAvaloniaProducts= --filter 'FullyQualifiedName~Plate|FullyQualifiedName~Raft|FullyQualifiedName~RenderPath|FullyQualifiedName~Deferred|FullyQualifiedName~ClipCap|FullyQualifiedName~LayerRange|FullyQualifiedName~SupportRenderMesh|FullyQualifiedName~Workspace|FullyQualifiedName~UiRefreshNumeric|FullyQualifiedName~Expression|FullyQualifiedName~ConfigViewModel|FullyQualifiedName~UserConfig|FullyQualifiedName~IsolationHeight|FullyQualifiedName~ViewportToolbar|FullyQualifiedName~WindowKeymap|FullyQualifiedName~ModeScoped|FullyQualifiedName~ObjectCommandScope|FullyQualifiedName~ProjectFile|FullyQualifiedName~NewProject|FullyQualifiedName~SliceCommand|FullyQualifiedName~ObjectPosition|FullyQualifiedName~UndoStack' --nologo
```
Final refined renderer checks (50 passed):
```powershell
dotnet test tests/Danslicer.Tests -c Release --no-build --no-restore --filter 'FullyQualifiedName~ClipCap|FullyQualifiedName~PlateReadability|FullyQualifiedName~Raft|FullyQualifiedName~RenderPath' --nologo
```
Final actual command results, not inferred: solution build exit 0; renderer result.txt and no error.txt; main workspace-ok.txt/view-effects-ok.txt and no workspace-error.txt; controls capture-ok.txt; git diff --check passed. Git status clean at source checkpoint; documentation changes are committed as the final descendant. No later task or main merge.


## User refinement — header, AO controls and compact cube
Worktree C:/Users/plane/.codex/worktrees/e262/Danslicer-chatgpt, branch codex/ui-refresh-04. Clean starting/last-good handoff: acbff390c73835cfde7235e8c7159047888f4281. User requested Auto drop at the left of the workspace strip, Layout/Support/Slicing centered, accessible AO configuration, and the attached compact cube with XYZ arrows. The image is a style reference, not geometry authority. Cast shadows on the bust were explicitly requested for discussion.

Moved the existing AutoDropEnabled toggle out of Layout options to the strip (visible in every mode, enabled in Layout as before). Equal-width outer grid columns keep workspace tabs geometrically centered at normal/narrow widths. Machine/resin moves to the second row in narrow windows. AO strength and world-mm radius now sit directly below the AO checkbox in View settings, using existing filled scrub/expression fields and one saved edit callback; no support-settings callback or support-geometry mutation is involved. Existing Preferences fields remain.

Replaced rendered face words/grid with a small dark neutral cube, fine edge strokes and colored X/Y/Z arrows with upright letters. Cube face/edge/corner snapping, drag gain, size setting and shared Rect/rail clearance remain. Arrow endpoints/letters also snap to positive-axis views. Enlarged subpixel strokes after inspecting real GL output. Legacy face-label data/helpers remain for compatibility but are not drawn.

Validation: Release solution build passed, existing warnings only. 127 focused cube/placement/render-config/plate tests passed, including new axis-label picking. Native main smoke passed: normal/narrow center alignment, Auto drop binding, AO expressions and cm-to-mm conversion, persisted values and prior settings/isolation contracts. GL framebuffer harness completed 64 views without errors/fallback; six new cube screenshots on both paths (above/below/top), and prior effect/picking/cap checks passed. Reviewed header, AO popout, narrow header and actual GL cube. Evidence in task-04-refinement-ui and task-04-refinement-gl (kept cube screenshots and full result log; unchanged non-cube images remain in task-04-after). No physical DPI/input or alternate GPU claims.

Shadow discussion outcome: user chose subtle shadows by default plus a stronger optional presentation mode. Proposed one soft-light system with strength/softness controls, a readable ambient floor for support work, and independent AO. This revision does NOT implement cast/self-shadows on models; current plate shadows/AO/cavity are unchanged. Record this design for the next shadow implementation discussion/work. No task 05 or merge.


## Authorized shadow system — implementation plan
Clean start: 2f3e48e1c18298989812bd2f37221b83fc20fc72, same e262 worktree/codex/ui-refresh-04. User explicitly authorized the previously discussed optional shadow system. Implement camera-relative soft directional cast/self-shadows, subtle Working default and stronger Presentation option, Off mode, independent mode strengths/softness and independent AO. Share a bounded shadow-map pass between Classic/Deferred; preserve unshadowed ambient readability, plate behavior, clipping, picking and UI contracts. Validate actual framebuffer Off/Working/Presentation, self/cross-object contacts, below/isolation/transparent support scenes and performance; isolate config. No core geometry/slicing changes, no task 05/main merge.


## Authorized shadow system — completed and verified
Implemented a shared camera-relative directional shadow map for Classic and Deferred opaque models/supports, including self-shadows. View settings has Off, Working (default 0.22 strength / 0.6 mm softness), Presentation (0.50 / 1.2 mm). Separate persisted strength/softness values for each mode use filled expression/unit fields. Off disables the fields and skips shadow rendering; zero strength is equivalent. AO and Cap values remain independent. No support geometry/slicing changes.

Depth-only 2048-square map (typically about 16 MiB storage), 48 fixed disk samples, bounded softness and strength, clipped casters and normal/receiver-plane depth bias. During actual framebuffer review, a square tap grid showed bands; disk sampling removed those. Screen-derived receiver gradients caused Deferred flat-face acne; analytic light-space receiver planes with snapped texel-centre comparison fixed it. A pixel regression now checks illuminated top/front patches. Strength never removes more than 70% of existing illumination. Ghost/transparent/overlay geometry is excluded, plate shading stays separate, and unsupported shadow target setup logs and disables the feature for the session. No temporal history or global-illumination claim. The shared light follows camera orientation.

Final verification: Release solution build passed (Danslicer.slnx); 115 focused tests passed, including 8 new shadow mode/config/light projection cases. Native MainWindow harness passed mode switching, independent mode persistence, arithmetic/unit editing, Off field state and AO/cap independence, plus existing workspace/settings contracts. Actual ANGLE GLES3 / RTX4090 framebuffer harness passed 84 views: Off/Working/Presentation/hard/zero, cross-object and single-mesh self-shadows, clipped/below/transparent cases, dense 600-support scene, prior plate/AO/cavity/cap/picking checks and new flat-face acne checks. No error.txt or fallback. Inspected Working/Presentation, Classic/Deferred, below and View controls. Evidence: evidence/task-04-shadows-gl and evidence/task-04-shadows-ui. Retained all final screenshots and result logs; deleted only intermediate captures produced during this iteration.

Performance is host/GPU-specific CPU submission plus glFinish, not isolated GPU timestamps: Deferred Off/Working/Presentation medians 0.20/0.46/0.57 ms, Presentation p95 15.63 ms. Classic Off/Working/Presentation medians 0.24/15.54/15.54 ms, on p95 about 16.1 ms. Dense scene medians Deferred 0.32 ms / Classic 15.50 ms, with scheduling spikes in the log. Do not extrapolate these to another GPU or a complete print job. Map is regenerated per frame; Off is the performance escape hatch.

Limits: this validates synthetic fixtures rather than the reference bust. Fixed shadow-map resolution limits tiny detail across large layouts. Deferred painted caps are screen-space closure and do not become extra shadow casters; source meshes still honor clipping. Soft studio appearance is approximate. Existing build warnings remain; no alternate-GPU/physical-input certification. One solution command initially used nonexistent Danslicer.sln, corrected to .slnx; final build passed. No task 05/main merge. Commit exact handoff reference in final response and resolve HEAD in future recovery.


## Settings follow-up — all visual effects configurable/off
User requested the effects also be configurable and switchable off in Settings. Preferences > Viewport now exposes model shadow Off/Working/Presentation and both presets' strength/softness, AO enable/strength/radius, reflections enable/strength, plate shadows enable, cavity enable/ridge/valley/radius, and cube enable/size. Settings and View popout share persisted ViewportConfig values; Preferences changes synchronize the popout/menu and redraw immediately. Numeric presets remain editable while Off so turning an effect off preserves tuning. Corrected the old below-plate hint to describe perimeter opacity and zero=hidden.

Display-only fields use ViewportSaved, separate from Saved (which applies settings to selected supports). The new regression checks persistence/events and preserved tuning without support-save side effects. Existing AO/reflection/cube-size/perimeter setters also use the display-only path. No renderer/shader changes in this follow-up.

Validation: Release solution build passed; 16 targeted config/shadow/plate tests passed. Native Preferences binding checks passed for shadow Off synchronizing to View settings, and AO/reflections/plate shadows/cavity/cube off persisting independently; original choices restored in isolated config. Existing workspace/settings checks passed. Preferences section navigation now scrolls to the section top rather than its bottom when the section is taller than the window. Final UI evidence: evidence/task-04-viewport-preferences. Renderer unchanged, so no redundant GL rerun.


## Final requested slider interaction
Filled sliders now map the pointer's track position directly across Minimum/Maximum, using the actual inner track width rather than Step per pixel. Step remains the keyboard increment; Shift provides fine relative motion. Click jitter still opens expression editing; previews remain local and release creates one commit. Non-filled unbounded model fields retain relative scrubbing. All scrub-field labels/editors show at most two decimal places and new commits round to two places (integer fields remain integers). Float representation noise does not create spurious unchanged saves. Existing expression/unit parsing, range rejection and undo/cancel behavior remain.

Verification: 41 numeric/config/expression tests passed, including direct pointer/range mapping, track resizing, fine motion and decimal rounding. Release solution build passed. Native workspace harness checks real filled-slider pointer midpoint, no saves during preview, exactly one on release, typed precision and unchanged float-backed edits; evidence in task-04-slider-final. Renderer unchanged. This is the user's final requested change for this task; no later task/main merge.
